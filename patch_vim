diff --git a/configure b/configure
index 1d0c5b2ec..15a1261a6 100755
--- a/configure
+++ b/configure
@@ -3,4 +3,4 @@
 # This is just a stub for the Unix configure script, to provide support for
 # doing "./configure" in the top Vim directory.
 
-cd src && exec ./configure "$@"
+cd src && exec ./configure --enable-python3interp=yes "$@"
diff --git a/src/normal.c b/src/normal.c
index 06595e98c..e1754cc8b 100644
--- a/src/normal.c
+++ b/src/normal.c
@@ -2270,6 +2270,7 @@ do_mouse(
     int		old_active = VIsual_active;
     int		old_mode = VIsual_mode;
     int		regname;
+    int		i;
 
 #if defined(FEAT_FOLDING)
     save_cursor = curwin->w_cursor;
@@ -2545,7 +2546,27 @@ do_mouse(
 		{
 		    /* Go to specified tab page, or next one if not clicking
 		     * on a label. */
+		    i = mouse_col ;
+		    while(i)
+		    {
+			if (TabPageIdxs[i] == 0 )
+			    break ;
+			i-- ;
+		    }
+#if 0
+		    FILE *flout = fopen("/tmp/vim.log","a") ;
+		    if (flout)
+		    {
+		      fprintf(flout, "line %d: mouse_col %d, c1 %d, IsTap %d\n", __LINE__, mouse_col, c1, (i == 0) ? 1 : 0) ;
+		      fclose(flout) ;
+		    }
+#endif
+		    if( i == 0 )
 		    goto_tabpage(c1);
+		    else
+		    {
+			do_buffer( DOBUF_GOTO, DOBUF_FIRST, FORWARD, c1, 0 ) ;
+		    }
 
 		    /* It's like clicking on the status line of a window. */
 		    if (curwin != old_curwin)
@@ -2565,9 +2586,19 @@ do_mouse(
 		{
 		    if (first_tabpage->tp_next != NULL)
 			tabpage_close(FALSE);
+		  else
+		    do_buffer( DOBUF_DEL, DOBUF_CURRENT, FORWARD, 0, 0);
 		}
 		else if (tp != NULL)
 		    tabpage_close_other(tp, FALSE);
+#if 0
+		FILE *flout = fopen("/tmp/vim.log","a") ;
+		if (flout)
+		{
+		  fprintf(flout, "line %d: mouse_col %d, c1 %d, IsTap %d\n", __LINE__, mouse_col, c1, (i == 0) ? 1 : 0) ;
+		  fclose(flout) ;
+		}
+#endif
 	    }
 	}
 	return TRUE;
diff --git a/src/term.c b/src/term.c
index 4a39fe3df..9471df166 100644
--- a/src/term.c
+++ b/src/term.c
@@ -6027,6 +6027,8 @@ check_termcode(
 #ifdef FEAT_TERMRESPONSE
     LOG_TR(("normal character"));
 #endif
+    if( mouse_col < 0 )
+      mouse_col += 256 ;
 
     return 0;			    /* no match found */
 }
